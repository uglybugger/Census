{
    "Application": {
        "Name": "Hipster Census React",
        "Version": "${REACT_APP_APPLICATION_VERSION}"
    },
    "Api": {
        "Endpoint": "${REACT_APP_API_ENDPOINT}"
    },
    "Logging": {
        "Environment": "${REACT_APP_SEQ_ENVIRONMENT}",
        "LogEventLevel": "Verbose",
        "Seq": {
            "Uri": "#${REACT_APP_SEQ_ENDPOINT}",
            "ApiKey": "${REACT_APP_SEQ_APIKEY}"
        }
    }
}